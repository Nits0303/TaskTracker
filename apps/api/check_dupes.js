const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.user.findMany().then(users => { const emails = users.map(u => u.email); console.log(emails); const lower = emails.map(e => e.toLowerCase()); const dupes = lower.filter((item, index) => lower.indexOf(item) !== index); console.log('Duplicates:', dupes); }).finally(() => prisma.$disconnect());

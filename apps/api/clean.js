const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const workspaces = await prisma.workspace.findMany(); for (const w of workspaces) { if (w.logoUrl && w.logoUrl.includes('?')) { await prisma.workspace.update({ where: { id: w.id }, data: { logoUrl: w.logoUrl.split('?')[0] } }); } } } run().then(() => console.log('Done')).finally(() => prisma.$disconnect());
